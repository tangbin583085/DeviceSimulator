# 安全说明

## 如何报告

正式 GitHub 仓库创建后，请优先使用 Private Security Advisory。不要把密钥、私有协议、生产地址或客户数据贴到公开 Issue。

模拟器默认监听 `127.0.0.1`。改成 `0.0.0.0` 或其他非 loopback 地址后，网络里的其他机器也可能访问，请只在可信环境中这样配置。

JSON 只能保存数据。如果配置可以触发脚本、外部进程、任意动态库加载、读取配置路径之外的内容，或者绕过资源上限，应按安全问题处理。
